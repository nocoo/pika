{
  "name": "pika",
  "version": "0.8.4",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run dev:api",
    "dev:api": "bun run --cwd packages/api dev",
    "dev:web": "bun run --cwd packages/web dev",
    "dev:web-worker": "bun run --cwd packages/web-worker dev",
    "dev:all": "bun scripts/dev-all.ts",
    "build": "bun run --filter '*' build",
    "build:web": "bun run --cwd packages/web build",
    "deploy:web-worker": "bun run --cwd packages/web-worker deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "tsc --noEmit && tsc --noEmit -p packages/api/tsconfig.json && tsc --noEmit -p packages/web/tsconfig.json && tsc --noEmit -p packages/web-worker/tsconfig.json",
    "lint:typecheck": "tsc --noEmit && tsc --noEmit -p packages/api/tsconfig.json && tsc --noEmit -p packages/web/tsconfig.json && tsc --noEmit -p packages/web-worker/tsconfig.json",
    "lint:biome": "bunx biome check packages/",
    "lint:secrets": "gitleaks detect --source . --no-git --config .gitleaks.toml",
    "lint:deps": "osv-scanner scan --lockfile=bun.lock --config .osv-scanner.toml",
    "lint:all": "bun run lint && bunx biome check packages/ && bun run lint:secrets && bun run lint:deps",
    "sync-versions": "bun scripts/sync-versions.ts --write",
    "prepare": "husky",
    "typecheck": "tsc --noEmit && tsc --noEmit -p packages/api/tsconfig.json && tsc --noEmit -p packages/web/tsconfig.json && tsc --noEmit -p packages/web-worker/tsconfig.json"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.9",
    "@vitest/coverage-v8": "^3.1.1",
    "husky": "^9.1.7",
    "typescript": "^5.8.2",
    "vitest": "^3.1.1"
  },
  "overrides": {
    "undici": "^7.24.0",
    "cookie": ">=0.7.0",
    "picomatch": ">=4.0.4",
    "fast-xml-parser": ">=5.5.9"
  },
  "dependencies": {
    "vite": "^8.0.7"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx,json}": [
      "biome check --error-on-warnings --no-errors-on-unmatched"
    ]
  }
}
